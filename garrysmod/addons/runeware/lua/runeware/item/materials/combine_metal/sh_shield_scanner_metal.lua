if !item.enabled then return; end
local info          = {};
info.name     = "shield scanner metal";
info.mdl      = "models/gibs/shield_scanner_gib2.mdl";
info.category = "combine metal";
item.add(info);